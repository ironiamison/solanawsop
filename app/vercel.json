{
  "framework": "nextjs",
  "buildCommand": "prisma generate && DATABASE_URL=file:./prisma/vercel.db prisma db push --skip-generate && next build --webpack",
  "installCommand": "npm install",
  "regions": ["iad1"]
}
